<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>SNEDU | Home</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <style>
    body {
      font-family: sans-serif;
      margin: 0;
    }
    .container {
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 16px;
    }
    .header {
      display: flex; 
      justify-content: center; /* 가로 가운데 정렬 */
      align-items: center; /* 세로 가운데 정렬 */
      width: 100%;
      background-color: #4285F4; 
      color: white;
      padding: 10px 0;
    }
    .header-content { /* 텍스트와 로고를 감싸는 컨테이너 */
      display: flex;
      align-items: center; 
    }
    .logo { /* 로고 스타일 */
      width: 50px; /* 로고 크기 조정 */
      margin-right: 10px; /* 로고와 텍스트 사이 간격 */
      padding: 5px; /* 배경을 위한 패딩 추가 */
      background-color: white; /* 배경색 설정 */
      border-radius: 5px; /* 둥근 모서리 설정 */
    }
    .title {
      font-size: 18px;
      font-weight: bold;
    }
    .subtitle { 
      font-size: 14px; 
    }
    .status {
      display: flex;
      justify-content: space-around;
      align-items: center;
      width: 100%;
      margin-top: 20px;
      background-color: #EEEEEE; /* 밝은 회색 배경 추가 */
      border-radius: 20px; /* 둥근 모서리 추가 */
      padding: 15px; /* 내부 여백 추가 */
    }
    .status-item {
      text-align: center;
    }
    .progress {
      width: 200px;
      height: 10px;
      background-color: #E9ECEF; /* 회색 배경 */
      border-radius: 5px;
      margin-bottom: 10px;
    }
    .progress-bar {
      height: 10px;
      background-color: #EF5350; /* 빨간색 진행바 */
      border-radius: 5px;
      width: <%=overallPercentage%>%; /* 진행률에 따라 변경 */
    }
    .buttons {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 16px;
      width: 100%;
      margin-top: 20px;
    }
    .button {
      display: flex;
      flex-direction: column; /* 아이콘과 텍스트를 세로로 배치 */
      justify-content: center;
      align-items: center;
      height: 120px;
      background-color: #F8F9FA; /* 밝은 회색 배경 */
      border-radius: 20px;
      text-decoration: none;
      color: black;
    }
    .button button {
      border: none;          /* 테두리 없애기 */
      background-color: transparent;  /* 배경 투명하게 */
      color: white;          /* 글자 색상 흰색 */
      padding: 0;            /* 기본 패딩 제거 */
      font-size: 16px;      /* 폰트 크기 유지 */
      font-weight: bold;     /* 폰트 굵기 유지 */
      cursor: pointer;       /* 마우스 포인터 모양 유지 */
    }
    .button-icon {
      width: 60px; /* 아이콘 크기 */
      margin-top: 10px;
    }
    .button-label {
      font-size: 16px;
      font-weight: bold;
      margin-top: 10px; /* 아이콘과 텍스트 사이 간격 */
    }
    .button:nth-child(1) { background-color: #4285F4; color: white; } /* 빨간색 버튼 */
    .button:nth-child(2) { background-color: #EF5350; color: white; } /* 파란색 버튼 */
    .button:nth-child(3) { background-color: #73A580; color: white; } /* 초록색 버튼 */
    .button:nth-child(4) { background-color: #3B577C; color: white; } /* 어두운 회색 버튼 */

    /* PC 화면에 적용될 스타일 */
    @media (min-width: 768px) {
      .container {
        width: 750px; /* 컨테이너 너비 설정 */
        margin: 0 auto; /* 가운데 정렬 */
        padding: 50px; /* 패딩 조정 */
      }
      .title {
        font-size: 24px; /* 제목 크기 조정 */
      }
      .button {
        height: 150px; /* 버튼 높이 조정 */
      }
      .button-label {
        font-size: 20px; /* 버튼 텍스트 크기 조정 */
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
        <div class="header-content">
            <img src="/images/snedu_logo.png" alt="Logo" class="logo">
            <div> 
              <div class="title">Student Nurse Education</div>
              <div class="subtitle">BLS-Provider 교육용 애플리케이션</div> 
            </div>
        </div>
    </div>
    <div class="status">
        <div class="status-item">
          <div><%= userName %> 님</div> <div>학습 현황</div>
          <div class="progress">
            <div class="progress-bar"></div>
          </div>
          <div><%= overallPercentage %>%</div>
        </div>
    </div>
    <div class="buttons">
      <form action="/docs" method="post" class="button">
        <input type="hidden" name="authorization" value="<%= authorization %>">
        <button type="submit">
          <div>
            <img src="images/bls_data_icon.png" alt="이론학습 아이콘" class="button-icon">
          </div>
          <div>
            <span class="button-label">이론학습</span>
          </div>
        </button>
      </form>
      <form action="/play" method="post" class="button">
        <input type="hidden" name="authorization" value="<%= authorization %>">
        <button type="submit">
          <div>
            <img src="images/education_video_icon.png" alt="교육영상 아이콘" class="button-icon">
          </div>
          <div>
            <span class="button-label">교육영상</span>
          </div>
        </button>
      </form>
      <form action="/history" method="post" class="button">
        <input type="hidden" name="authorization" value="<%= authorization %>">
        <button type="submit">
          <div>
            <img src="images/record_icon.png" alt="기록 아이콘" class="button-icon">
          </div>
          <div>
            <span class="button-label">기록</span>
          </div>
        </button>
      </form>
      <form action="/" method="get" class="button">
        <input type="hidden" name="authorization" value="<%= authorization %>">
        <button type="submit">
          <div>
            <img src="images/logout.png" alt="로그아웃 아이콘" class="button-icon">
          </div>
          <div>
            <span class="button-label">로그아웃</span>
          </div>
        </button>
      </form>
    </div>
  </div>
</body>
</html>